sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t unnicognizant/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true unnicognizant/react-test npm test -- --coverage

after_success:
  - docker build -t unnicognizant/multi-client ./client
  - docker build -t unnicognizant/multi-nginx ./nginx
  - docker build -t unnicognizant/multi-server ./server
  - docker build -t unnicognizant/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push unnicognizant/multi-client
  - docker push unnicognizant/multi-nginx
  - docker push unnicognizant/multi-server
  - docker push unnicognizant/multi-worker
